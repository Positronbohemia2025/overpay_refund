import { DocumentPage } from './DocumentPage';

/** Sample report route (T015 skeleton; real content in T035). */
export default function SampleReport() {
  return (
    <DocumentPage title="Raport-model">
      <p>Conținutul raportului-model este adăugat în T035.</p>
    </DocumentPage>
  );
}
