import { DocumentPage } from './DocumentPage';

/** Data-handling route (T015 skeleton; real content in T036, aligns with §5). */
export default function DataHandling() {
  return (
    <DocumentPage title="Cum tratăm datele">
      <p>Conținutul detaliat despre tratarea datelor este adăugat în T036.</p>
    </DocumentPage>
  );
}
