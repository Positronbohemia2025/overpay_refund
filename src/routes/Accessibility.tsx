import { DocumentPage } from './DocumentPage';

/** Accessibility statement route (T015 skeleton; real content in T036). */
export default function Accessibility() {
  return (
    <DocumentPage title="Declarație de accesibilitate">
      <p>Declarația de accesibilitate este adăugată în T036.</p>
    </DocumentPage>
  );
}
