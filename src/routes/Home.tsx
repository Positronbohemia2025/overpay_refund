/**
 * The landing page (T015 skeleton; composed in T037/T045/T054).
 * Sections are added in canonical order (FR-001) as each phase lands.
 */
export default function Home() {
  return (
    <>
      {/* US1 sections (Hero → HowItWorks → WhoWeAreFunding → ContractHandling → SiteFooter)
          are composed here in T037. */}
    </>
  );
}
