import { DocumentPage } from './DocumentPage';

/** About route (T015 skeleton; real content in T036). */
export default function About() {
  return (
    <DocumentPage title="Despre noi">
      <p>Conținutul paginii „Despre noi” este adăugat în T036.</p>
    </DocumentPage>
  );
}
